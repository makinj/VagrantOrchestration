# -*- mode: ruby -*-
# vi: set ft=ruby :

project_name = 'boxes/base'


Dir.glob("#{prov_path}/*/vagrant.rb") {|x| require(x)}

Vagrant.configure("2") do |vagrant|

  vagrant.vm.box = "vmutti/base"
  vagrant.disksize.size = '64GB'

  helper = VagrantHelper.new(vagrant, prov_path, config_path, vault_path, project_path)

  helper.vb do |vb|
    vb.memory = "6144"
    vb.name = project_name.gsub(/\//,'-')
  end

  pre_install(helper)
  #autochrome_install(helper)
  #chrome_install(helper)
  #docker_install(helper)
  #ethereum_install(helper)
  #golang_install(helper)
  #katoolin_install(helper)
  #samba_install(helper)
  #ssh_key_install(helper)
  #sublime_install(helper)
  #xfce_install(helper)
  post_install(helper)
end

